[project]
name = "wishlist-bd"
version = "1.0.0"
description = "Wishlist for Ballsdex v3."
authors = [{name = "haymooed"}]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["wishlist_app"]
