[project]
name = "santa-bd"
version = "1.0.0"
description = "A little extra package for Ballsdex v3, just some Santa stuff."
authors = [
    {name = "ariel-aram"},
    {name = "haymooed"}
]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["santa_app"]
