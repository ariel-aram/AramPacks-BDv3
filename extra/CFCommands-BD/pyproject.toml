[project]
name = "cfcommands-bd"
version = "1.0.0"
description = "Additional commands for Ballsdex v3."
authors = [
    {name = "ariel-aram"},
    {name = "Caylies"}
]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cfcommands_app"]
