[project]
name = "museum-bd"
version = "1.0.0"
description = "A museum display extra package for Ballsdex v3 — showcase your favorite cards."
authors = [{name = "haymooed"}]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["museum_app"]
