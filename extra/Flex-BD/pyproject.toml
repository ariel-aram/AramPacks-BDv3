[project]
name = "flex-bd"
version = "1.0.0"
description = "Flex command for Ballsdex v3 - submit your MarketDex items for moderator approval and showcase."
authors = [
    {name = "ariel-aram"},
    {name = "haymooed"}
]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["flex_app"]
