[project]
name = "moderation-bd"
version = "1.1.5"
description = "Just a moderation extra package for Ballsdex v3, what else did you guess, duh."
authors = [{name = "molteencreates"}]
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
